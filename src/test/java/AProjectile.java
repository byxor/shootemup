import static org.junit.Assert.*;
import org.junit.*;
import shootemup.geometry.Vector;
import shootemup.Projectile;


public class AProjectile {

    // @Test
    // public void spawnsWhereItIsToldTo() {
    // 	Point position = new Point(0, 0);
    // 	Projectile projectile = new Projectile(position);
    // 	assertEquals(position, projectile.position());
    // }
}
